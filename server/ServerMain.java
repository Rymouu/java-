package server;
// Main.java (dans le package server)
public class Main {
    public static void main(String[] args) {
        Server server = new Server();
        server.run(); // Appel de la méthode main de la classe Server pour lancer le serveur
    }
}
